# Docker

The root `docker-compose.yml` starts PostgreSQL, Redis, the NestJS API, and the Next.js web app for local development.

```bash
docker compose up
```

Mobile development should run through Expo on the host machine so the app can connect to a simulator or device.
